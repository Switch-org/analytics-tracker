# Skip audit for dev dependencies during install/publish
# These vulnerabilities are in dev dependencies (semantic-release, npm itself)
# and do not affect the published package
audit=false
fund=false
